<<<<<<< SEARCH
                // If latest is clear or stale, opacity should be 0.1 so it doesn't clutter.
                if (isLatestClearOrStale) {
                    fillOpacity = 0.3; // Much lower base opacity for clear/stale
                } else if (latestAgeHours < 0.33) {
=======
                // If latest is clear or stale, opacity should be 0.1 so it doesn't clutter.
                // We only render one polygon for clear/stale per location (handled above),
                // so we don't need to worry about multiple 0.1s stacking.
                if (isLatestClearOrStale) {
                    fillOpacity = 0.3; // Much lower base opacity for clear/stale
                } else if (latestAgeHours < 0.33) {
>>>>>>> REPLACE
