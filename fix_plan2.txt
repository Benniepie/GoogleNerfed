Ah, wait.
In `test_preload.js`, NO request to `/api/sentinel-latest/` was made at load!
So WHY did `verify_issues2.js` see a request?
Let's look at what `verify_issues2.js` does:
1) Wait 2s
2) `console.log("Current zoom:", zoom);` -> `Current zoom: 6`
3) *Wait! I see the logs:*
`Current zoom: 6`
`[Network] Fetching S2 Latest tile: http://localhost:3000/api/sentinel-latest/10/600/351.webp`
... these 6 fetches happened... wait, `10/600/351.webp` ? At zoom 6, x and y are usually around 30-40. 600 and 351 are coordinates for zoom 10.
Wait! Leaflet thinks the map is at zoom 11 for the layer, so it requests zoom 10 (because zoomOffset = -1).
BUT WHY DID LEAFLET REQUEST IT?
Because `minZoom: 11` means "Do not show this layer if map zoom is < 11".
BUT my `s2latest` layer in `map-core.js`:

```javascript
			sentinelLayer: L.tileLayer('/api/sentinel-latest/{z}/{x}/{y}.webp', {
			tileSize: 512,
				minZoom: 11,
				maxZoom: 15,
...
```

Ah! `minZoom: 11` on the `L.tileLayer` means: "I only have tiles for zoom 11 and above. If the map is zoomed to 10, DO NOT FETCH."
Wait! If `minZoom: 11`, why did it fetch?
Because in `verify_issues2.js`:
```javascript
    // Check zoom level
    let zoom = await page.evaluate(() => window.map.getZoom());
    console.log("Current zoom:", zoom);

    // Switch to s2latest layer
    await page.evaluate(() => {
        const input = document.querySelector('input[name="basemap"][value="s2latest"]');
        input.checked = true;
        input.dispatchEvent(new Event('change'));
    });
```
Wait! The network fetches happened BETWEEN `Current zoom: 6` and `Switched to s2latest layer` or AFTER?
No, the order of console logs from Node.js is asynchronous.
Actually, maybe they happened AFTER switching?
Let's check the console logs:
```
Current zoom: 6
[Network] Fetching S2 Latest tile...
[Network] Fetching S2 Latest tile...
Switched to s2latest layer
```
The node console log `Switched to s2latest layer` happens AFTER the page evaluation finishes, but the network request logging comes via events, which might be interleaved!
So it switched, THEN it fetched!
BUT the map was at Zoom 6! Why did it fetch if `minZoom: 11`?
Wait! `minZoom` on a TileLayer is ONLY respected if `maxNativeZoom` is not conflicting, OR perhaps Leaflet ignores it if the layer is in a LayerGroup?
Let's check:
```javascript
var group = L.layerGroup([sentinel]).addTo(map);
```
Wait, if it's in a `L.layerGroup`, `L.LayerGroup` just proxies `addTo(map)`.
Let's see Leaflet docs:
If `minZoom` is set, and map zoom is < `minZoom`, `L.TileLayer` doesn't load tiles.
BUT wait!
If `minZoom` is 11, and `zoomOffset` is -1.
What does `zoomOffset` do? It ADDS to the zoom level used for the URL, BUT does it affect `minZoom` check?
Let's check my test_minzoom.html!
